options(papersize = "a4")
options(tab.width = 4)
options(width = 130)
options(digits = 4)
options(prompt = "> ")
options(continue = "+ ")
local({
    r <- getOption("repos")
    r["CRAN"] <- "https://mirrors.tuna.tsinghua.edu.cn/CRAN/"
    options(repos = r)
})
#library(ggplot2)
