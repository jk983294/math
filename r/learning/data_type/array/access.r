result <- array(1:12, dim = c(2, 3, 2))

result[2, , 2]  # the 2nd row of the second matrix of the array
result[1, 3, 1]  # the element in the 1st row and 3rd column of the 1st matrix
result[, , 2]  # the 2nd Matrix
