x <- c("why", "video", "cross", "extra", "deal", "authority")

# returns an integer vector that returns the desired order
str_order(x, decreasing = FALSE)

(sorted <- str_sort(x, decreasing = FALSE))
